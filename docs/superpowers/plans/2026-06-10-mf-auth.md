# BFF → Web 认证模块联邦化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Web 端登录/注册从"跳转 BFF 完整页面"改为"Web 通过 Module Federation 动态加载 BFF 暴露的 AuthPage 组件"，消除整页跳转、保留 SPA 状态、解耦 BFF 的页面渲染职责。

**Architecture:** BFF 拆为两套构建：(1) 保留 Next.js 跑 API (`/api/auth/*`)；(2) 新增 Rspack 单独打 MF remote bundle，输出 `public/mf-auth/`，由 Next.js 自动作为静态资源托管。Web (Vite host) 通过 `@module-federation/vite` 在 `/login` 路由 dynamic import 远端，共享 react/react-dom/@fitness/ui-components/lucide-react 为 singleton。

**Tech Stack:** `@module-federation/enhanced` (runtime) + `@module-federation/vite` (Vite host plugin) + `@module-federation/rsbuild-plugin` 或 Rspack 内置 `ModuleFederationPlugin` (remote) + `concurrently` (并行 dev) + Next.js 16 / Vite 8 / React 19。

**Spec:** `docs/superpowers/specs/2026-06-10-mf-auth-design.md`

---

## 文件结构总览

新增/修改的文件分布：

**BFF（apps/bff/）**
- 新建：`rspack.config.mjs` — Rspack MF remote 构建配置
- 新建：`src/remote/bootstrap.ts` — remote 入口
- 新建：`src/app/(auth)/auth/_components/AuthPage/index.tsx` — 抽出的 AuthPage 组件
- 新建：`src/app/(auth)/auth/_components/AuthPage/index.module.css` — AuthPage 容器/卡片/标题/底部样式
- 修改：`src/app/(auth)/auth/page.tsx` — 改为薄壳（Phase 1）/ 删除（Phase 3）
- 删除：`src/app/(auth)/auth/auth.module.css`（Phase 1 迁出后删除）
- 修改：`next.config.ts` — 加 CORS headers
- 修改：`package.json` — 加 rspack/@module-federation/enhanced 依赖、dev:remote/build:remote 脚本

**Web（apps/web/）**
- 修改：`vite.config.ts` — 加 @module-federation/vite 插件
- 修改：`package.json` — 加 @module-federation/enhanced、@module-federation/vite
- 修改：`src/pages/Login/index.tsx` — 用 dynamic import 替换 `window.location.href`，加加载失败兜底

**保留不动**
- `apps/bff/src/app/(auth)/auth/_components/BrandingSection/`
- `apps/bff/src/app/(auth)/auth/_components/LoginForm/`
- `apps/bff/src/app/(auth)/auth/_components/RegisterForm/`
- `apps/bff/src/app/(auth)/auth/_hooks/useAuthMode.ts`
- `apps/web/src/hooks/useAuth.ts`、`apps/web/src/services/authService.ts`（已有现成的 login + token + navigate 逻辑）
- `apps/web/src/routes/index.tsx`（路由表本身不变）

---

## Phase 1 — 抽 AuthPage 组件（无 MF，零风险）

> 目标：把 `page.tsx` 的逻辑下沉到 `_components/AuthPage/`，让 BFF 现有的 `/auth` 路由继续工作（仅作内部重构），为后续 MF 暴露做准备。

### Task 1.1: 创建 AuthPage 组件

**Files:**
- Create: `apps/bff/src/app/(auth)/auth/_components/AuthPage/index.tsx`

- [ ] **Step 1: 创建文件，写入组件代码**

完整代码：

```tsx
'use client';

import { useState } from 'react';
import { Dumbbell } from 'lucide-react';
import { cn } from '@fitness/ui-components';
import BrandingSection from '../BrandingSection';
import LoginForm, { type AuthSuccessPayload } from '../LoginForm';
import RegisterForm from '../RegisterForm';
import { useAuthMode } from '../../_hooks/useAuthMode';
import styles from './index.module.css';

interface AuthPageProps {
  /** 登录/注册成功后由父组件决定 token 存储与跳转 */
  onSuccess: (data: AuthSuccessPayload) => void;
}

export default function AuthPage({ onSuccess }: AuthPageProps) {
  const [error, setError] = useState('');
  const { isLogin, animating, animDirection, switchMode } = useAuthMode();

  const handleSwitchMode = (toLogin: boolean) => {
    setError('');
    switchMode(toLogin);
  };

  return (
    <>
      <BrandingSection />

      <div className={styles.container}>
        <div className={cn(styles.card, isLogin ? styles.loginHeight : styles.registerHeight)}>
          <div className={styles.logo}>
            <Dumbbell className={styles.logoIcon} />
          </div>

          <div className={styles.header}>
            <div
              className={cn(
                styles.titleWrapper,
                animating && (animDirection === 'up' ? styles.slideUp : styles.slideDown),
              )}
            >
              <h1 className={styles.title}>{isLogin ? '欢迎回来' : '创建账户'}</h1>
              <p className={styles.subtitle}>
                {isLogin ? '登录您的账户继续健身之旅' : '开启您的健身之旅'}
              </p>
            </div>
          </div>

          {error && <div className={styles.error}>{error}</div>}

          <div
            className={cn(
              styles.formContainer,
              animating && (animDirection === 'up' ? styles.fadeUp : styles.fadeDown),
            )}
          >
            {isLogin ? (
              <LoginForm onError={setError} onSuccess={onSuccess} />
            ) : (
              <RegisterForm onError={setError} onSuccess={onSuccess} />
            )}
          </div>

          <div className={styles.footer}>
            <span className={styles.footerText}>
              {isLogin ? '还没有账户？' : '已有账户？'}
            </span>
            <button type="button" onClick={() => handleSwitchMode(!isLogin)} className={styles.link}>
              {isLogin ? '立即注册' : '立即登录'}
            </button>
          </div>
        </div>
      </div>
    </>
  );
}
```

**关键变化**（vs 当前 `page.tsx`）：
- 抽出 `onSuccess` 为 prop（不再内联 `localStorage` + `router.push`）
- 内部拥有 `error` state（不再依赖父组件传入）
- 不再 `import { useRouter } from 'next/navigation'`（保持 BFF/agnostic）

- [ ] **Step 2: 创建对应的 CSS Module**

完整代码到 `apps/bff/src/app/(auth)/auth/_components/AuthPage/index.module.css`：

```css
.container {
  @apply flex items-center justify-center p-[24px] relative overflow-hidden;
  min-height: 100vh;
}

.container::before {
  content: '';
  position: absolute;
  top: -50%;
  left: -50%;
  width: 200%;
  height: 200%;
  background: radial-gradient(circle at 30% 70%, hsl(20 100% 55% / 0.08) 0%, transparent 50%),
              radial-gradient(circle at 70% 30%, hsl(20 100% 55% / 0.05) 0%, transparent 40%);
  animation: float 20s ease-in-out infinite;
}

@keyframes float {
  0%, 100% { transform: translate(0, 0) rotate(0deg); }
  33% { transform: translate(2%, 2%) rotate(1deg); }
  66% { transform: translate(-1%, 1%) rotate(-1deg); }
}

.card {
  position: relative;
  width: 100%;
  max-width: 440px;
  background: white;
  border-radius: 24px;
  padding: 48px 40px;
  box-shadow:
    0 4px 6px -1px rgba(0, 0, 0, 0.03),
    0 10px 20px -2px rgba(0, 0, 0, 0.04),
    0 25px 50px -12px rgba(0, 0, 0, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.8);
  overflow: hidden;
  transition: height 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
  height: auto;
}

.card.loginHeight { min-height: 520px; }
.card.registerHeight { min-height: 620px; }

.header {
  text-align: center;
  margin-bottom: 36px;
  height: 80px;
  overflow: hidden;
  position: relative;
}

.titleWrapper {
  transition: transform 0.45s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.35s ease-out;
  position: absolute;
  width: 100%;
  left: 0;
}

.titleWrapper.slideUp { transform: translateY(-50px); opacity: 0; }
.titleWrapper.slideDown { transform: translateY(50px); opacity: 0; }

.logo {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 56px;
  height: 56px;
  background: linear-gradient(135deg, hsl(20 100% 55%) 0%, hsl(25 100% 50%) 100%);
  border-radius: 16px;
  margin-bottom: 20px;
  box-shadow: 0 8px 20px -4px hsl(20 100% 55% / 0.4);
}

.logoIcon { width: 28px; height: 28px; color: white; }

.title {
  font-size: 26px;
  font-weight: 700;
  color: hsl(222 47% 11%);
  margin-bottom: 8px;
  letter-spacing: -0.02em;
}

.subtitle {
  font-size: 15px;
  color: hsl(215 16% 47%);
  line-height: 1.5;
}

.error {
  background: hsl(0 84% 60% / 0.1);
  color: hsl(0 84% 55%);
  font-size: 14px;
  font-weight: 500;
  padding: 14px 18px;
  border-radius: 12px;
  margin-bottom: 24px;
  text-align: center;
  border: 1px solid hsl(0 84% 60% / 0.2);
  animation: shake 0.4s ease-out;
}

@keyframes shake {
  0%, 100% { transform: translateX(0); }
  20% { transform: translateX(-4px); }
  40% { transform: translateX(4px); }
  60% { transform: translateX(-4px); }
  80% { transform: translateX(4px); }
}

.formContainer {
  transition: transform 0.45s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.35s ease-out;
}

.formContainer.fadeUp { transform: translateY(30px); opacity: 0; }
.formContainer.fadeDown { transform: translateY(-30px); opacity: 0; }

.footer {
  text-align: center;
  margin-top: 28px;
  padding-top: 24px;
  border-top: 1px solid hsl(214 32% 91%);
  font-size: 14px;
  color: hsl(215 16% 47%);
}

.footerText { display: inline; }

.link {
  color: hsl(20 100% 55%);
  font-weight: 600;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
  margin-left: 6px;
  transition: all 0.2s;
  position: relative;
}

.link::after {
  content: '';
  position: absolute;
  bottom: -2px;
  left: 0;
  width: 0;
  height: 2px;
  background: hsl(20 100% 55%);
  transition: width 0.25s ease-out;
}

.link:hover::after { width: 100%; }
.link:hover { color: hsl(20 100% 50%); }
```

**关键变化**：所有原本在 `auth.module.css` 中、由 `page.tsx` 使用的样式（除已迁到子组件的外）全部移到这里。

- [ ] **Step 3: 提交**

```bash
git add apps/bff/src/app/\(auth\)/auth/_components/AuthPage/
git commit -m "refactor(bff): 抽出 AuthPage 组件到 _components/AuthPage

为 Module Federation 暴露做准备：AuthPage 现在接受 onSuccess 回调，
内部管理 error 状态，不再依赖 next/navigation 或 localStorage。"
```

### Task 1.2: 改造 page.tsx 为薄壳

**Files:**
- Modify: `apps/bff/src/app/(auth)/auth/page.tsx`

- [ ] **Step 1: 替换 page.tsx 内容**

把当前 87 行的 `page.tsx` 整体替换为：

```tsx
'use client';

import { useRouter } from 'next/navigation';
import AuthPage, { type AuthSuccessPayload } from './_components/AuthPage';

export default function Page() {
  const router = useRouter();

  const handleAuthSuccess = (data: AuthSuccessPayload) => {
    localStorage.setItem('accessToken', data.accessToken);
    localStorage.setItem('refreshToken', data.refreshToken);
    router.push('/');
  };

  return <AuthPage onSuccess={handleAuthSuccess} />;
}
```

- [ ] **Step 2: 删除不再需要的 `auth.module.css`**

```bash
rm apps/bff/src/app/\(auth\)/auth/auth.module.css
```

> 该文件中残留的 `.brandingSection` 重复 CSS（上次拆分时遗留）也一并清理。

- [ ] **Step 3: 启动 dev server 验证**

```bash
cd apps/bff && pnpm dev
```

浏览器访问 `http://localhost:3000/auth`，手动验证：
- 看到登录表单（手机号 + 密码 + 登录按钮）
- 切换"立即注册" → 出现 4 个输入框
- 输入错误密码 → 看到错误提示，shake 动画
- 输入正确凭据 → 跳到 `/` 且 `localStorage` 有 token

- [ ] **Step 4: type check**

```bash
cd apps/bff && pnpm exec tsc --noEmit 2>&1 | grep -E "auth/(page|_components/AuthPage)" || echo "✓ AuthPage 无类型错误"
```

预期：`✓ AuthPage 无类型错误`（pre-existing 错误在 `src/app/api/[...path]/route.ts` 与本次无关）

- [ ] **Step 5: 提交**

```bash
git add apps/bff/src/app/\(auth\)/auth/
git commit -m "refactor(bff): /auth 路由改为薄壳，仅做 next router 适配"
```

### Task 1.3: Phase 1 收尾验证

- [ ] **Step 1: 全量构建**

```bash
cd /Users/lplusm/Code/Self/Projects/fitness-instructor && pnpm -r build 2>&1 | tail -20
```

预期：所有包构建成功（BFF 仍可能因 `src/app/api/[...path]/route.ts` 的 pre-existing 类型错误失败，与本次重构无关——若失败请确认是否新增错误）

- [ ] **Step 2: 浏览器最终冒烟**

按 Task 1.2 Step 3 的清单过一遍，确认行为完全一致。

---

## Phase 2 — BFF 加 Rspack 远端构建

> 目标：让 BFF 同时能打一个 MF remote bundle 输出到 `public/mf-auth/`，与 Next.js 共存。

### Task 2.1: 安装依赖

**Files:**
- Modify: `apps/bff/package.json`

- [ ] **Step 1: 安装 Rspack 与 MF 运行时**

```bash
cd apps/bff && pnpm add -D @rspack/core @rspack/cli
cd apps/bff && pnpm add @module-federation/enhanced
```

**注意**：`@module-federation/enhanced` 必须 Web / BFF 锁同一版本（Phase 4 会再次安装）

- [ ] **Step 2: 安装 concurrently 用于并行 dev**

```bash
cd apps/bff && pnpm add -D concurrently
```

- [ ] **Step 3: 验证 package.json 内容**

```bash
cat apps/bff/package.json | grep -E "@rspack|module-federation|concurrently"
```

预期看到（版本号以实际安装为准）：

```json
"@module-federation/enhanced": "^x.x.x",
"@rspack/cli": "^x.x.x",
"@rspack/core": "^x.x.x",
"concurrently": "^x.x.x"
```

- [ ] **Step 4: 提交**

```bash
git add apps/bff/package.json apps/bff/pnpm-lock.yaml
git commit -m "build(bff): 加 Rspack + Module Federation + concurrently 依赖"
```

### Task 2.2: 创建 Rspack 配置

**Files:**
- Create: `apps/bff/rspack.config.mjs`

- [ ] **Step 1: 写入完整配置**

```js
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { defineConfig } from '@rspack/cli';
import { ModuleFederationPlugin } from '@module-federation/enhanced/rspack';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const monorepoRoot = path.resolve(__dirname, '../..');

export default defineConfig({
  context: __dirname,
  entry: './src/remote/bootstrap.ts',
  output: {
    path: path.resolve(__dirname, 'public/mf-auth'),
    publicPath: 'auto',
    clean: true,
  },
  resolve: {
    extensions: ['.ts', '.tsx', '.js', '.jsx'],
    alias: {
      '@fitness/ui-components': path.resolve(monorepoRoot, 'packages/ui-components/src'),
    },
  },
  module: {
    rules: [
      {
        test: /\.tsx?$/,
        loader: 'builtin:swc-loader',
        options: {
          jsc: {
            parser: { syntax: 'typescript', tsx: true },
            transform: { react: { runtime: 'automatic' } },
          },
        },
      },
      {
        test: /\.module\.css$/,
        type: 'css/module',
      },
      {
        test: /\.css$/,
        type: 'css',
      },
    ],
  },
  plugins: [
    new ModuleFederationPlugin({
      name: 'bff_auth',
      filename: 'remoteEntry.js',
      exposes: {
        './AuthPage': './src/remote/bootstrap.ts',
      },
      shared: {
        react: { singleton: true, requiredVersion: '^19.0.0', eager: false },
        'react-dom': { singleton: true, requiredVersion: '^19.0.0', eager: false },
        'lucide-react': { singleton: true, eager: false },
        '@fitness/ui-components': { singleton: true, eager: false },
      },
    }),
  ],
  experiments: {
    css: true,
    outputModule: true,
  },
});
```

**说明：**
- `resolve.alias` 把 `@fitness/ui-components` 显式指向 `packages/ui-components/src`（避免 Rspack 与 Next.js 解析差异）
- CSS Modules 用 Rspack 内置 `experiments.css: true` + `type: 'css/module'`，无需额外装 style-loader/css-loader
- `outputModule: true` 启用 ESM 输出，配合 Vite 端的 `type: 'module'`

- [ ] **Step 2: 创建 remote 入口**

**Files:**
- Create: `apps/bff/src/remote/bootstrap.ts`

```ts
export { default } from '../app/(auth)/auth/_components/AuthPage';
```

- [ ] **Step 3: 加 npm 脚本**

**Files:**
- Modify: `apps/bff/package.json`

把 `scripts` 段更新为：

```json
"scripts": {
  "dev": "concurrently -n next,rspack -c blue,green \"next dev\" \"pnpm dev:remote\"",
  "dev:remote": "rspack watch",
  "build": "pnpm build:remote && next build",
  "build:remote": "rspack build",
  "start": "next start",
  "lint": "eslint"
}
```

- [ ] **Step 4: 跑一次 build 验证产物**

```bash
cd apps/bff && pnpm build:remote
```

预期：命令退出码 0，输出 `apps/bff/public/mf-auth/remoteEntry.js`

```bash
ls -la apps/bff/public/mf-auth/
```

预期看到：

```
remoteEntry.js
src_bootstrap_ts.js
src_bootstrap_ts.js.map
...（若干 mf 共享依赖 chunk）
```

- [ ] **Step 5: 提交**

```bash
git add apps/bff/rspack.config.mjs apps/bff/src/remote/bootstrap.ts apps/bff/package.json
git commit -m "build(bff): 加 Rspack MF remote 构建配置，输出 public/mf-auth/"
```

### Task 2.3: 验证 dev watch 模式

- [ ] **Step 1: 启动并行 dev**

```bash
cd apps/bff && pnpm dev
```

预期：看到两条并行输出，rspack watch 启动后写入 `public/mf-auth/`

- [ ] **Step 2: 验证文件存在**

```bash
ls apps/bff/public/mf-auth/remoteEntry.js
```

预期：文件存在

- [ ] **Step 3: 触发重建（修改 AuthPage 任一行）**

```bash
# 在编辑器里保存一次 AuthPage/index.tsx，等几秒
ls -la apps/bff/public/mf-auth/
```

预期：`remoteEntry.js` 的 mtime 刷新

- [ ] **Step 4: 关闭 dev server**

`Ctrl+C` 终止

---

## Phase 3 — BFF 加 CORS + 删除 /auth 路由

> 目标：让 `/mf-auth/*` 走 CORS，并把 Web 不再需要的 `/auth` SSR 页面彻底移除。

### Task 3.1: 加 CORS headers

**Files:**
- Modify: `apps/bff/next.config.ts`

- [ ] **Step 1: 读取当前 next.config.ts 全文**

```bash
cat apps/bff/next.config.ts
```

- [ ] **Step 2: 加 headers 配置**

在 `next.config.ts` 现有导出后追加 `async headers()` 函数（保留原有配置）：

```ts
import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // ... 保留原有配置
  async headers() {
    return [
      {
        source: '/mf-auth/:path*',
        headers: [
          {
            key: 'Access-Control-Allow-Origin',
            value: process.env.WEB_ORIGIN ?? 'http://localhost:5173',
          },
          { key: 'Access-Control-Allow-Methods', value: 'GET, OPTIONS' },
          { key: 'Access-Control-Allow-Headers', value: '*' },
        ],
      },
    ];
  },
};

export default nextConfig;
```

> `WEB_ORIGIN` env 让生产环境可以指向真实 Web 域名

- [ ] **Step 3: 启动 BFF dev 验证 CORS**

```bash
cd apps/bff && pnpm dev
```

另起一个 terminal：

```bash
curl -I http://localhost:3000/mf-auth/remoteEntry.js
```

预期响应中包含：

```
Access-Control-Allow-Origin: http://localhost:5173
```

- [ ] **Step 4: 关闭 dev server**

`Ctrl+C` 终止

### Task 3.2: 删除 /auth 路由

**Files:**
- Delete: `apps/bff/src/app/(auth)/auth/page.tsx`
- Delete: `apps/bff/src/app/(auth)/` (整个目录)

- [ ] **Step 1: 删除目录**

```bash
rm -rf apps/bff/src/app/\(auth\)
```

- [ ] **Step 2: 启动 dev server**

```bash
cd apps/bff && pnpm dev
```

- [ ] **Step 3: 验证 /auth 返回 404**

```bash
curl -I http://localhost:3000/auth
```

预期：`HTTP/1.1 404 Not Found`

- [ ] **Step 4: 验证 /api/auth/login 仍正常**

```bash
curl -X POST http://localhost:3000/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"phonenumber":"13800138000","password":"wrong"}'
```

预期：返回 `4xx` 错误（业务错误，不是 404 路由错误），证明 API 路由未受影响

- [ ] **Step 5: 关闭 dev server 并提交**

```bash
git add -A apps/bff/src/app/
git commit -m "refactor(bff): 删除 /auth SSR 路由；AuthPage 改为仅通过 MF remote 暴露"
```

### Task 3.3: Phase 3 收尾验证

- [ ] **Step 1: 启动 dev 并 curl 全部端点**

```bash
cd apps/bff && pnpm dev
```

另起 terminal：

```bash
# API 仍然工作
curl -I http://localhost:3000/api/auth/login
# MF 远端仍然可达
curl -I http://localhost:3000/mf-auth/remoteEntry.js
# /auth 404
curl -I http://localhost:3000/auth
```

预期：

| 路径 | 状态 |
|---|---|
| `/api/auth/login` | 405 Method Not Allowed（POST-only，正常） |
| `/mf-auth/remoteEntry.js` | 200 + `Access-Control-Allow-Origin` 头 |
| `/auth` | 404 |

- [ ] **Step 2: 关闭 dev server**

`Ctrl+C`

---

## Phase 4 — Web 接入 MF

> 目标：让 Web 的 `/login` 路由通过 dynamic import 加载 BFF 的 AuthPage，替代 `window.location.href`。

### Task 4.1: 安装依赖

**Files:**
- Modify: `apps/web/package.json`

- [ ] **Step 1: 安装**

```bash
cd apps/web && pnpm add @module-federation/enhanced
cd apps/web && pnpm add -D @module-federation/vite
```

**注意**：版本号必须与 BFF 的 `@module-federation/enhanced` 一致！

- [ ] **Step 2: 对比两端版本**

```bash
cd /Users/lplusm/Code/Self/Projects/fitness-instructor
grep -E '"@module-federation/enhanced"' apps/bff/package.json apps/web/package.json
```

预期两边版本完全一致：

```
apps/bff/package.json:    "@module-federation/enhanced": "^x.x.x",
apps/web/package.json:    "@module-federation/enhanced": "^x.x.x",
```

如不一致，统一到 BFF 端的版本：`cd apps/web && pnpm add @module-federation/enhanced@<version>`

- [ ] **Step 3: 提交**

```bash
git add apps/web/package.json apps/web/pnpm-lock.yaml
git commit -m "build(web): 加 @module-federation/vite + enhanced 依赖"
```

### Task 4.2: 配置 Vite MF host

**Files:**
- Modify: `apps/web/vite.config.ts`

- [ ] **Step 1: 读取当前配置**

```bash
cat apps/web/vite.config.ts
```

- [ ] **Step 2: 替换为 MF host 配置**

```ts
import { defineConfig, loadEnv } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';
import { federation } from '@module-federation/vite';

export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, process.cwd(), '');
  const mfRemoteUrl =
    env.VITE_MF_REMOTE_URL ?? 'http://localhost:3000/mf-auth/remoteEntry.js';

  return {
    plugins: [
      react(),
      tailwindcss(),
      federation({
        name: 'web',
        remotes: {
          bff_auth: {
            type: 'module',
            name: 'bff_auth',
            entry: mfRemoteUrl,
            entryGlobalName: 'bff_auth',
            shareScope: 'default',
          },
        },
        shared: {
          react: { singleton: true, requiredVersion: '^19.0.0' },
          'react-dom': { singleton: true, requiredVersion: '^19.0.0' },
          'lucide-react': { singleton: true },
          '@fitness/ui-components': { singleton: true },
        },
      }),
    ],
    server: {
      proxy: {
        '/bff': {
          target: 'http://host.docker.internal:3000',
          changeOrigin: true,
        },
        '/users': {
          target: 'http://host.docker.internal:3001',
          changeOrigin: true,
        },
      },
    },
  };
});
```

- [ ] **Step 3: 提交**

```bash
git add apps/web/vite.config.ts
git commit -m "build(web): vite.config 接入 @module-federation/vite 插件，声明 host + 共享依赖"
```

### Task 4.3: 改造 LoginPage

**Files:**
- Modify: `apps/web/src/pages/Login/index.tsx`

- [ ] **Step 1: 读取当前内容**

```bash
cat apps/web/src/pages/Login/index.tsx
```

- [ ] **Step 2: 替换为 dynamic import 版本**

```tsx
import { useEffect, useState } from 'react';
import type { ComponentType } from 'react';
import { useAuth } from '../../hooks/useAuth';

interface AuthSuccessPayload {
  accessToken: string;
  refreshToken: string;
}

type AuthPageProps = {
  onSuccess: (data: AuthSuccessPayload) => void;
};

type AuthPageComponent = ComponentType<AuthPageProps>;

export default function LoginPage() {
  const { login } = useAuth();
  const [AuthPage, setAuthPage] = useState<AuthPageComponent | null>(null);
  const [loadError, setLoadError] = useState<string | null>(null);

  const loadAuthPage = async () => {
    setLoadError(null);
    try {
      const mod = await import('bff_auth/AuthPage');
      setAuthPage(() => mod.default);
    } catch (err) {
      const message = err instanceof Error ? err.message : '未知错误';
      setLoadError(message);
    }
  };

  useEffect(() => {
    loadAuthPage();
  }, []);

  const handleAuthSuccess = (data: AuthSuccessPayload) => {
    login(data.accessToken, data.refreshToken);
  };

  if (loadError) {
    return (
      <div className="flex flex-col items-center justify-center min-h-screen gap-4 p-6">
        <p className="text-red-500 text-center">
          认证模块加载失败：{loadError}
        </p>
        <button
          onClick={loadAuthPage}
          className="px-6 py-2 bg-orange-500 text-white rounded-lg hover:bg-orange-600 transition"
        >
          点击重试
        </button>
      </div>
    );
  }

  if (!AuthPage) {
    return (
      <div className="flex items-center justify-center min-h-screen">
        <p className="text-gray-500">加载中...</p>
      </div>
    );
  }

  return <AuthPage onSuccess={handleAuthSuccess} />;
}
```

**关键点**：
- 使用现有 `useAuth().login()` 替代手动 `localStorage.setItem` + `navigate('/')`
- `import('bff_auth/AuthPage')` 是 dynamic import，由 Vite + MF 插件编译为远端加载
- 加载失败有兜底 UI + 重试按钮

- [ ] **Step 3: 提交**

```bash
git add apps/web/src/pages/Login/index.tsx
git commit -m "feat(web): LoginPage 改为 dynamic import 加载 MF 远端 AuthPage，加载失败兜底"
```

### Task 4.4: 端到端验证

- [ ] **Step 1: 启动 BFF（带 Rspack watch）**

```bash
cd apps/bff && pnpm dev
```

- [ ] **Step 2: 另起 terminal，启动 Web**

```bash
cd apps/web && pnpm dev
```

- [ ] **Step 3: 浏览器打开 Web 登录页**

访问 `http://localhost:5173/login`

预期：
- 短暂看到"加载中..."
- 随后渲染完整的登录卡片（BrandingSection + 手机号 + 密码 + 登录按钮）
- 视觉上与之前 BFF 渲染的 `/auth` 一致

- [ ] **Step 4: 提交表单**

输入测试凭据，提交。

预期：
- 按钮显示"登录中..."
- 成功后 `localStorage` 出现 `accessToken` 和 `refreshToken`
- 浏览器地址栏变成 `http://localhost:5173/`（**仍是 Web 的 `/`，不是 BFF 的**）

- [ ] **Step 5: 验证 React DevTools 单例**

打开 React DevTools，选中任意组件。

预期：组件树中 `react`、`@fitness/ui-components` 只有一个实例（MF shared singleton 生效）

- [ ] **Step 6: 验证 React DevTools 网络**

打开浏览器 Network 面板，刷新 `/login`。

预期看到（按顺序）：
1. `http://localhost:5173/` 入口 HTML
2. `http://localhost:3000/mf-auth/remoteEntry.js`（MF 入口）
3. `http://localhost:3000/mf-auth/src_bootstrap_ts.js`（AuthPage chunk）
4. 若干 `http://localhost:3000/mf-auth/mf-*.js`（共享依赖 chunk）

- [ ] **Step 7: 验证切换注册模式**

点击"立即注册"，确认 4 个输入框 + 注册按钮 + 切换动画正常。

- [ ] **Step 8: 验证错误路径**

故意输入错误密码提交，确认错误提示 + shake 动画 + 按钮恢复可点。

### Task 4.5: 验证加载失败兜底

- [ ] **Step 1: 关闭 BFF**

`Ctrl+C` 终止 BFF dev server。

- [ ] **Step 2: 强制刷新 Web /login**

浏览器 `Cmd+Shift+R` 硬刷新 `http://localhost:5173/login`。

预期：
- 看到"认证模块加载失败：..."提示
- 看到"点击重试"按钮

- [ ] **Step 3: 重启 BFF 后点击重试**

```bash
cd apps/bff && pnpm dev
```

等几秒后，浏览器点击"点击重试"。

预期：登录卡片正常出现。

- [ ] **Step 4: 关闭 dev servers**

`Ctrl+C` 终止 BFF 和 Web

### Task 4.6: 提交 Phase 4

- [ ] **Step 1: 检查所有改动已 commit**

```bash
cd /Users/lplusm/Code/Self/Projects/fitness-instructor && git status
```

预期：clean（所有改动已在前序步骤 commit）

---

## Phase 5 — 清理与文档（可选）

### Task 5.1: 更新根 README 或 apps/web/README（如存在）

- [ ] **Step 1: 检查根 README**

```bash
ls README.md apps/web/README.md apps/bff/README.md 2>&1
```

- [ ] **Step 2: 如果存在，加入 MF 段**

在 README 的"开发"或"架构"小节追加：

```markdown
### 登录页通过 Module Federation 加载

BFF 暴露 `AuthPage` 组件作为 MF remote，输出到 `apps/bff/public/mf-auth/`。
Web 端通过 `import('bff_auth/AuthPage')` 动态加载，无需跳转。

开发环境：
- BFF dev server 在 :3000，托管 MF 远端与 API
- Web Vite dev server 在 :5173，通过 MF 插件从 BFF 拉远端
- 修改 AuthPage 源码后需在 Web 端硬刷新（MF remote 不走 Vite HMR）

生产环境：
- `pnpm build` 在 BFF 端会先打 Rspack remote 再 `next build`
- `VITE_MF_REMOTE_URL` 控制 Web 端远端地址
```

### Task 5.2: 全量构建验证

- [ ] **Step 1: 跑全量 build**

```bash
cd /Users/lplusm/Code/Self/Projects/fitness-instructor && pnpm -r build 2>&1 | tail -30
```

预期：所有包构建成功（除 pre-existing 的 `src/app/api/[...path]/route.ts` 类型错误外）

- [ ] **Step 2: 检查 mf-auth 产物**

```bash
ls apps/bff/public/mf-auth/
ls apps/bff/.next/standalone/apps/bff/public/mf-auth/ 2>/dev/null
```

预期：两个目录都包含 `remoteEntry.js`（如果 standalone 输出启用）

- [ ] **Step 3: 跑 prod 验证**

```bash
cd apps/bff && pnpm start
# 另起 terminal
curl -I http://localhost:3000/mf-auth/remoteEntry.js
```

预期：返回 200 + CORS 头

- [ ] **Step 4: 关闭 prod server**

`Ctrl+C`

### Task 5.3: 关闭所有任务、最终提交

- [ ] **Step 1: 检查 git 状态**

```bash
git status
```

预期：clean 或仅有未追踪的 build 产物（这些在 .gitignore 中）

- [ ] **Step 2: 列出本次实施的所有 commit**

```bash
git log --oneline feature/lijm ^3c3a144 2>&1 | head -20
```

预期：包含 Phase 1-5 的所有 commit，与 spec 阶段对应

---

## 验收对照（spec 第四节）

| spec 验收项 | 对应 Task | 验证方式 |
|---|---|---|
| 所有包能构建 | Task 5.2 Step 1 | `pnpm -r build` 退出码 0 |
| MF 远端可达 | Task 3.3 Step 1 | `curl -I http://localhost:3000/mf-auth/remoteEntry.js` |
| Web 端到端 | Task 4.4 Step 4 | 浏览器提交 → Web `/` + `localStorage` 有 token |
| BFF `/auth` 已移除 | Task 3.2 Step 3 | `curl -I http://localhost:3000/auth` → 404 |
| 切换模式 | Task 4.4 Step 7 | 浏览器切换登录/注册 |
| 错误路径 | Task 4.4 Step 8 | 错误密码 → 表单错误显示 |
| 跨 bundle 单例 | Task 4.4 Step 5 | React DevTools 验证 |
